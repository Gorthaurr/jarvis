# Следующая сессия — хендофф

> 🔴 **ЧИТАТЬ ПЕРВЫМ: [docs/HOW_IT_WORKS.md](docs/HOW_IT_WORKS.md)** — механизмы + как тестировать текстом + мышление.

## ✅ ПЕРСОНА v36 — ГИБРИД EN-каркас/RU-эталоны + консолидация (2026-06-20)
По просьбе Антона («персона длинная + на русском, лучше каркас по-английски»). Переписал `persona.md`:
правила/каркас по-английски (token-эффективнее, чуть точнее следование Opus), **все русские примеры-
эталоны и 15 калибровочных реплик сохранены дословно** (это целевой тон вывода — переводить нельзя),
убраны дубли. **446→330 строк (−26%).** Грузится целиком как строка (`persona/index.ts loadPersona`),
секции не парсятся → рестрактур загрузку не ломает. `lang: ru` (вывод юзеру ВСЕГДА русский — правило
усилено в шапке). **Верифицировано 2 способами:** (1) адверсариал-агент диф старая↔новая — «потерь
критических поведений НЕТ, все 13 блоков + 15 реплик + реестр инструментов сохранены»; нашёл 2 улучшения
(добавлены теги `[sharply]/[brightly]` в набор — старая их юзала в эталонах, но в наборе не было; убран
устаревший «ElevenLabs v3» — проект на Yandex). (2) Живой QA на v36: длина-по-типу/антиципация/деликатное
несогласие/эмоция-по-запросу/остроумная отбивка абсурда/содержательная глубина — всё работает. Бэкап
старой: `/tmp/persona_v35_backup.md`.

## 🔴 СЕССИЯ 2026-06-20 — НАДЁЖНОЕ УПРАВЛЕНИЕ ПК (Антон зол: «мышью не может, две кнопки в игре не нажать, макросы сам не пишет»)
Тестировал в **Dota 2**. Заземлил всё на железе (не догадки). 2 воркфлоу-ресёрча (Deepgram persistent WS — спека `tasks/wbtv6gp86.output`, отложена; reliable-control/game-input/macros — план `tasks/wrnmesj1c.output`). **СДЕЛАН Спринт 1 (корень обоих симптомов), всё проверено на машине:**
- **A1 — DPI-манифест сайдкара был НЕВАЛИДЕН** (значения с переносами `\n  PerMonitorV2\n` → ОС не применяла → процесс DPI-unaware → координаты мазали на 125%). Фикс: значения в одну строку (`app.manifest`) + health-check в `Program.cs`. **VERIFIED на железе:** `DPI awareness=PER_MONITOR(V2)(2) virtualScreen=4480x1440 [OK]`.
- **Корень «рандомного промаха»:** скрин ужимается до 1568px (`screen.ts`), а `input_click` шлёт коорд. модели **сырыми как логические** — обратного маппинга нет → системный сдвиг (полный фикс A0 — следующий; макро-путь его обходит).
- **A2.1 — игры: клавиша down+up летела ОДНИМ SendInput-батчем** (`InputSynthesizer.PressCombo`) → Source2/RawInput (Dota) проглатывал = «две кнопки не нажать». Фикс: раздельные фронты + `HoldDelay` (env `JARVIS_KEY_HOLD_MS`=25 + джиттер). Собрано+republish.
- **SELF-МАКРОСЫ (явная просьба):** новый `data/macros/grounding.py` (DPI-пролог PER_MONITOR_V2 → `find_template` cv2 мульти-масштаб / `find_text` OCR rus+eng → `focus` окна → `step(do,verify,retry)` → честный abort). **VERIFIED:** `mss`==`pyautogui`==2560×1440 `aligned:true`; OCR нашёл 123 блока в Доте с координатами. pip: pydirectinput-rgx, pyscreeze, numpy, rapidfuzz, pytesseract (tesseract в системе есть). HERMES-навык «Писать надёжный макрос» (recall на «напиши макрос»/«для доты»/«запиши макрос»).
- **Персона v32:** «лестница управления ПК» (ui_invoke→UIA-клик→vision-грундинг→свой макрос) + **verify-after-act как ЗАКОН** + игры (бинды/консоль `autoexec.cfg` раньше эмуляции).
- Деплой: сайдкар republish (убил старый→собрал→поднял новый рестартом клиента), сервер v32, клиент переподключился. 52 теста (persona/skills/control) зелёные.
- **РАСПОЗНАВАНИЕ ВКЛАДОК (жалоба «не понимает про какую вкладку я говорю»):** новый инструмент `browser_tabs` (список открытых вкладок: заголовок/хост/active/**audible♪**) сквозь стек — `tab.list` в `background.js` → `ExtensionBridge.tabList` → dispatch `browserTabs` → схема в packages/tools (горячий) + ext-тип в dispatch.ts/agent/index.ts. Персона **v33**: «о вкладке неявно („эта/та“, „где играет музыка“) → сперва browser_tabs, потом действуй по её хосту; неоднозначно → переспроси». +2 теста (625 зелёных). Dev-эндпоинт `GET /ext/tabs`. **VERIFIED end-to-end на реальных вкладках:** Джарвис перечислил YouTube(active,♪)/Я.Музыка/YouTube и понял, откуда звук.
  - **ТОЧНЫЙ ТАРГЕТ ПО tabId (доделано):** `browser_tabs` показывает `[tabId N]` в выдаче; `browser_act`/`browser_read`/`browser_inspect` принимают `tabId` (схемы); `resolveBrowserTarget` даёт явному tabId высший приоритет + запоминает (follow-up на той же вкладке); ext `findTargetTab(tabId,"")` бьёт ТОЧНО в неё (ветка `!host`) даже при нескольких вкладках одного хоста. +1 тест (browser_act{tabId}→ext.tabAct 4-м арг.). Закрывает «две вкладки одного сайта».
  - **ЗАКРЫТИЕ ВКЛАДОК (гэп «не умеет закрывать»):** новый `browser_close` (`tab.close` в ext → `chrome.tabs.remove`; bridge `tabClose`; dispatch `browserCloseTab`; схема). Адресация: `tabId` (точно одну) / `url`-хост (все этого сайта) / без аргументов (активную). Персона **v35**: «открыть — browser_open, закрыть — browser_close». +3 теста (628 зелёных). **VERIFIED:** открыл example.com → `browser_close{url}` → 0 example-вкладок осталось. (Открытие `browser_open` уже работало.)
  - **МЕДИА-ДЕЙКСИС (персона v34):** короткие команды о медиа — «продолжи/дальше/пауза/стоп/перемотай/погромче» при открытом видео/♪ — действовать, не переспрашивать: browser_tabs→tabId (та, что ♪/активная видео-вкладка)→browser_act(play/pause/seek)→подтвердить по факту. «продолжи»=снять с паузы, НЕ вопрос «что продолжить». **VERIFIED:** «продолжи»→«Снял с паузы, Аватар играет»; «поставь паузу»→«Поставил на паузу, ролик остановлен» (browser_tabs→act→inspect-verify). Конфликта с §20 RESUME_WORDS нет (перехват только при активной задаче, router-ws:614).

### Очередь (план `wrnmesj1c`, отложено):
- **A0** — обратный маппинг координат скрин↔клик: `screen.ts` отдаёт scale/origin/phys; `dispatch.ts` пересчёт; `Ipc.cs ClickArgs+space=physical`→`ClickPhysical`. Чинит ПРЯМОЙ `input_click`.
- **A2.2** — относительная мышь (`MOUSEEVENTF_MOVE` без ABSOLUTE) для камеры/прицела в 3D-играх.
- **C0/C1** — Dota: Джарвис пишет `autoexec.cfg` (бинды) + GSI-листенер (состояние игры).
- **D1** — set-of-marks грундинг (нумеровать элементы → модель выбирает номер).
- ~~**Deepgram persistent WS**~~ — ✅ **СДЕЛАН + LIVE (2026-06-20):** `PersistentDeepgramConnection` + `LeasedTurn` в `deepgram.ts` за флагом `JARVIS_DEEPGRAM_PERSISTENT=1` (в `.env`, дефолт класса OFF). Один сокет на сессию (Finalize вместо CloseStream, gen-гейтинг по `activeTurn`, reconnect-replay буфера хода без дублей, KeepAlive в простое, idle-sleep 120с). +7 юнит-тестов фейк-WS (один сокет/нет склейки/late-drop/reconnect/barge-in/dispose), 623 серв.теста зелёные. **Живой прогон:** churn 55/54→**1/0**, РЕЧЬ ПОТЕРЯНА 29→**0**, 0 ошибок/склейки. Старый per-utterance путь сохранён (флаг=0 — мгновенный откат).

## ✅ СЕССИЯ 2026-06-19 (часть 3 — автономный QA-марафон, ~2ч) — СДЕЛАНО
Прогнал ~50 кейсов текст-драйвером (`_qa_battery.mjs`), сверил с персоной, нашёл и починил, всё verify (616 серв. тестов зелёные):
- **Персона v31** (глубокий ресёрч Джарвис+Альфред, воркфлоу): +Альфред-слой — **антиципация-вперёд** / **деликатное несогласие** («уверены, сэр?») / **understatement**. Проверено вживую: «удали всё с рабочего стола»→«Стоп, сэр… покажу сначала»; рейс в 18:00→считает таймлайн выезда. Разнообразие речи подтверждено (ack'и разные).
- **НОВЫЙ ФУНКЦИОНАЛ — «уважительная проактивность» (не мешать §9):** `pipeline.isUserBusy` держит НЕсрочную проактивную речь (итоги фоновых задач), пока юзер занят (звонок/полный экран/блокировка), отдаёт по освобождении (`drainPending`); напоминания (urgent) проходят всегда. Клиент шлёт `client.context` (реальный `locked` через powerMonitor; Sensors теперь инстанцируется — раньше НЕ слался). +3 теста.
- **Обучен навыкам (HERMES, в БД):** 5 навыков — смена аудио-устройства (гэп «переключи на Razer»), реферат/лекция, таблица Excel, закрыть зависшее, видео на YouTube. Recall работает (важно: навыки в skills-таблице БД, .md — только артефакт).
- **Латентность:** retrieval-таймаут текст/фон 2000→700мс (env `JARVIS_RETRIEVAL_TIMEOUT_MS`). Голос уже 350мс.
- **Очередь задач (нагруз-тест 6 задач):** cancel снимает всё, не виснет; при 5 одновр. Opus часть упиралась в watchdog→«связь прервалась» → `MAX_PARALLEL_TASKS` 5→3 (env-тюн).
- **Фулл код-ревью (воркфлоу, 17 агентов):** ~36 мёртвых элементов, 8 багов (verify). ПОЧИНЕНО: TTS-кеш игнорил emotion (зло/радость отдавались из кеша спокойными, **medium**); telegram_send_voice описание честно = аудио-файл, не кружок (**medium**); «готово» в STATUS_WORDS перехватывало «спасибо, всё готово»; tool-loop без парного tool_result при отмене (инвариант Anthropic); barge-in по одному speech_start минуя калибровку; SpendGuard drain не дожидался на shutdown. УДАЛЕНО: `tgSendVoiceInPage` (~230 строк мёртвой записи голосового), `DEFERRED_TOOLS`, mcp `catalogLines`/`schema`.
- **Открыто (мелочь, для уборки):** dead `classifyFailure`/`isWarm`/CDP-сообщение «12с»; фантом-фичи `messaging/contacts.ts` (дизамбигуация контактов — задокументирована, но не подключена) и `consent.revokeSend` — решить: подключить или удалить + поправить STATUS.md. tier0 жадно ловит «запусти ядерную ракету»→app_launch вместо остроумной отбивки.

## ✅ СЕССИЯ 2026-06-19 (часть 2 — отладка вживую через текст-драйвер) — СДЕЛАНО
**Создан текст-драйвер `_jarvis_cmd.mjs`** (гоняю Джарвиса командами без голоса, см. HOW_IT_WORKS §2) + dev-эндпоинты
`/ext/telegram_file`. Прогнал батарею → нашёл и починил 4 бага (все с тестами, 613 серв. зелёные):
- **Гигиена вывода:** `verbalize` чистит утёкший `<invoke>`-tool-call и аудио-теги `[warmly]` (текли в речь/чат).
- **Continuity на лету:** `activeForUser` + `formatActiveTasks` → «ещё считаю» вместо «ничего не делаю»; `freshContext`
  больше не обнуляет диалог (берёт `recentTurns(10)`) → вопрос-продолжение не теряет контекст; `speakResult` шлёт итог ТАКЖЕ в чат (§22).
- **Гейт диктора ВЫКЛ** (`JARVIS_SPEAKER_GATE=0`): биометрия ложно отклоняла голос ВЛАДЕЛЬЦА (скоры 0.03–0.7) → «не реагировал».
- **Wake fuzzy:** добавлены латинские ослышки (jarious/jarvias).
**Telegram (марафон):** ТЕКСТ в Telegram **РАБОТАЕТ** (tab-reuse + активация вкладки `openTgTab` + MV3-keepalive
`startKeepAlive` + надёжный клик по чату). **АУДИО-ФАЙЛ — СТЕНА:** webK блокирует программную загрузку файлов (защита,
синтетический change/drop игнорируются). Настоящее голосовое — только GramJS (api_id, который Telegram-цензура у Антона не даёт).
**Открыто:** Telegram-аудио ждёт api_id; persistent Deepgram WS; biometric-overhaul чтобы вернуть owner-only.

---

> Состояние на конец сессии **2026-06-18**. **НЕ закоммичено.** Архитектура/грабли — в памяти:
> `project_jarvis_architecture.md` (ПЕРВЫМ), `project_jarvis_hermes.md`, `project_jarvis_voice.md`,
> `project_jarvis_infra.md`, `feedback_universal.md`. Детали по каждому пункту — в секциях ниже.

---

# 🔴 СЕССИЯ 2026-06-19 — ХЕНДОФФ (читать ПЕРВЫМ; делать ДОЛГО И КАЧЕСТВЕННО, не патчами — `feedback_depth_over_speed`)

## Принцип работы (важно)
Антон: «**работать Джарвис быстро должен, а не ты — ты делай долго и качественно**». Корень, не симптом;
надёжно; verify (тесты + по возможности живой прогон). НЕ быстрые заплатки → «проверь». См. `feedback_depth_over_speed`.

## ✅ СДЕЛАНО 2026-06-19 (эта сессия) — ПАМЯТЬ/CONTINUITY #1 + MCP-арсенал, всё верифицировано
- **«Джарвис забывает ЧТО сделал» — ЗАКРЫТО (корень, не заплатка).** §20-реестр задач был in-memory →
  рестарт сервера (каждый деплой) стирал «что сделал» → на «сделал?» Джарвис не знал. ФИКС:
  - `brain/tasks/task-store.ts` (новый, зеркало working-store): снимок реестра в `data/tasks.json`
    (атомарно tmp→rename, дебаунс 300мс, **`flushTaskStores()` в gateway.close()** — спасает задачу,
    завершённую за <300мс до graceful-рестарта). `TaskManager.toJSON/restore/setOnChange/recentTerminal`.
  - **Честность restore:** не-терминальная задача (running/paused) после рестарта → `failed` («прервано
    перезапуском»), НЕ воскрешается как running (иначе соврал бы «всё ещё делаю»). active() её не вернёт.
  - **Инжект «недавно выполненные» в контекст** (`formatRecentTasks` → `UserContextSlot.recentTasks` →
    `renderDynamic` — НЕкешируемый хвост, кеш §15 не ломается; окно 6ч, env `JARVIS_TASK_RETENTION_MS`).
  - **Адверсариал-ревью (17 агентов, 9 находок — ВСЕ исправлены):** болтовня (stepsDone=0) больше не
    всплывает как «✓ задача» (фильтр `isSubstantiveTask`); flush на shutdown; `parseInt` retention (пустой
    env больше не схлопывает в 60с); NaN-коэрс времён в restore; дата прерванной = реальный startedAt (не
    вытесняет успехи); чистка осиротевшего .tmp. **+20 тестов, 604 серв. зелёные, typecheck чист, boot-构造 ок.**
  - ⚠️ На СЛУХ/живьём НЕ проверено (нужен живой клиент+мик): записать задачу с tool-use → рестарт сервера →
    «что ты делал?» должен ответить из реестра. Сервер PID 4484 крутит СТАРЫЙ код — **нужен рестарт, чтобы применить.**
- **MCP-арсенал #4 — РАСШИРЕН и LIVE.** Активны 2 сервера = **27 инструментов**: `think` (npx, 1) +
  **`github` (npx, 26: репы/файлы/issues/PR/поиск/push)** — токен `GITHUB_PERSONAL_ACCESS_TOKEN` в `.env`
  (сейчас токен gh CLI, scopes repo/gist/read:org). Проверено end-to-end через реальный loadMcpConfig+dotenv.
  Добавлена нормализация `uvx/uv→.exe` (config.normalizeCommand, +тест). git/fetch/time/postgres/playwright —
  в `mcp.json._disabled`. ⚠️ **uvx-сервера (Python) НЕ поднялись: кэш uv заклинен локом pywin32 (os error 32),
  конфликт с расширением windows-mcp**, которое держит pywin32 → нужен `uv cache clean --force` (когда
  windows-mcp простаивает) + последовательный прогрев `uvx mcp-server-<x>`, затем перенести в `servers`.
- **СЕРВЕР ПЕРЕЗАПУЩЕН** (был PID 4484 → теперь 16884) — фикс памяти и github MCP уже В БОЮ. Контекст диалога
  и реестр задач переживают это (диск-персист). MCP-инструменты — ХОЛОДНЫЕ (модель грузит схему по tool_load).

## 🟢 СДЕЛАНО ЭТУ СЕССИЮ (в бою, 573 серв.-теста зелёные; НЕ закоммичено)
- **Браузер через расширение — переписан надёжно:** `hostOf` чинит голый хост (был дрейф в Telegram);
  таргетинг по **tabId**+хост (не «активная»); акт без `browser_open` → честная ошибка; play/pause/click через
  **MAIN-world** (`robustClickMain` React-onClick/Enter — минует Swiper-гейт; `mediaControlMain` — состояние по
  медиа-элементу/mediaSession, не по первой кнопке). **Встряхнуть/пауза работают** (подтвердил Антон).
- **`browser_inspect`** (ГЛАЗА в DOM: интерактивные элементы + устойчивые селекторы) — общий приём для любого сайта.
- **Ленивая загрузка инструментов §15** (`COLD_TOOL_NAMES` + `tool_load` + кеш-блок `systemTools`) — арсенал без раздувания контекста.
- **MCP-HOST РАБОТАЕТ** (`brain/mcp/manager.ts`+`config.ts`, `mcp.json`): подключение MCP-серверов, неймспейс `mcp__*`,
  dispatch-роутинг, COLD-каталог. Проверено: сервер `think` (sequential-thinking) подключился. Добавить сервер = строка в mcp.json.
- **НЕ мешать активному пользователю** (`actuators/user-presence.ts isUserActive` + idle-gate на input.click/type/key);
  **мышь в вебе заблокирована** (только browser_act). **STT handshake-race** (close ждёт открытия WS). **barge-in порог 600→350** + диагностика rms.
  **Семантические заголовки чипов** (`actionTitle`). **Память на диск** (`working-store`, переживает рестарт) + грейс resume + scoped на Session.
- **seek** (перемотка через `media.currentTime`; forward/back перематывают видео, если есть плеер). Persona **v30**.

## 🔴 ОТКРЫТО — приоритет на след. сессию (по эффект/риск)
1. ✅ **ПАМЯТЬ/CONTINUITY (топ) — СДЕЛАНО эту сессию** (персист TaskManager + осознание «сделал?», см. выше).
   Остаток: (а) живая приёмка на слух после рестарта сервера; (б) `persistent Deepgram WS` (#5) — корень
   churn/«не слышит»; (в) episodic-запись итогов задач НЕ делал намеренно (вернуло бы «странные графики» —
   память осознанная). tier0-открытия (ютуб и т.п.) идут мимо реестра §20 → в «сделал?» не попадают (отдельно).
2. **Голосовые в Telegram (`telegram_send_voice`)** — построено по исходникам tweb (click-toggle, см. `docs/CALLS_PLAN.md`),
   честная верификация есть, НО Антон говорит «не уходит». Нужен ЖИВОЙ диагностический прогон (на провале функция
   отдаёт дамп DOM кнопок webK) → добить флоу записи; ИЛИ перейти на **GramJS** (надёжный кружок, разовый apiId/вход).
   Голос — **только filipp** (требование Антона). Файлы: `apps/extension/background.js` (`tgSendVoiceInPage`).
3. **seek** — задеплоено; **проверить живьём на YouTube** («перемотай на 20 сек»).
4. **MCP-арсенал** — host готов; подключить реальные сервера (git/github/fetch/postgres/playwright) в `mcp.json`;
   проброс image-результатов MCP (сейчас сводится к тексту). `docs/MCP_PLAN.md`.
5. **Persistent Deepgram WS (боль #1)** — всё ещё открыто: пререквизит живых звонков + корень «не слышит»/латентности.
6. **Звонки (живой режим)** — спроектировано (`docs/MCP_PLAN.md` нет — `docs/CALLS_PLAN.md`): MVP = TTS→вирт.микрофон
   (нужен VB-CABLE), живой диалог блокирован латентностью (сначала persistent STT WS).

**Планы-доки этой сессии:** `docs/MCP_PLAN.md` (MCP-арсенал), `docs/CALLS_PLAN.md` (звонки/голосовые, голос=filipp).
**Воркфлоу-результаты (машинные):** `tasks/wbc91qv4r.output` (память — дочитать), `tasks/wzmmg5qvw.output` (MCP),
`tasks/w6fy1lyi9.output` (звонки), `tasks/wyzq3o4dk.output` (tweb голосовое), `tasks/wy2i3i1x0.output` (DOM-глаза).
**Грабли запуска:** сервер `npx tsx src/index.ts` (НЕ watch); расширение reload — `curl -X POST http://127.0.0.1:8787/ext/reload`;
клиент — пересборка `node scripts/build.mjs` + electron. Память переживает мои рестарты (диск-персист).

---


## 📋 ОСТАВШИЕСЯ ПЛАНЫ (индекс — что делать дальше, по приоритету)
> За сессии 2026-06-17/18 сделано ~50 фиксов (диагностика «шляпа-документ», адаптивная речь, угловые
> чипы задач, intent edit-vs-new, фулл-аудит 28+ багов, reliability-P0, аудит-P0/P1 батчами, пол М/Ж
> + надёжность гейта). Всё с тестами, зелёное (≈470 серв.+71 клиент). НЕ закоммичено.

**A. Идентификация голоса до идеала** (см. секцию «ИДЕНТИФИКАЦИЯ ГОЛОСА 2026-06-18»):
  1. AS-Norm + калибровка порога по EER/DET (нужна когорта impostor-эмбеддингов + офлайн-тулза; нужен живой голос Антона). — главный ROI к надёжности.
  2. UI-тумблер «реагировать только на мои голоса» вместо env `JARVIS_SPEAKER_GATE` (client+protocol+router+персист).
  3. F0-guard в reject/null-ветке (хранить пол владельца при enroll); «слышу: Антон»/«не узнан»; многофразовый enroll UI.
  4. Банк k=5-8 векторов (max-cos) + гистерезис 2-из-3 + окно доверия 8с; probe-сегментация (единая нарезка enroll/identify).

**B. Связь/надёжность** (см. «СВЯЗЬ/НАДЁЖНОСТЬ»):
  1. Persistent Deepgram WS на разговор (не переоткрывать каждый ход — churn ~0.3-0.5с/ход). [Deepgram reconnect-in-stream УЖЕ сделан.]
  2. Устойчивость к таймаутам Anthropic на ТЯЖЁЛОМ ходе (генерация документа/кода): стрим/больший max_tokens, чтобы стаб не убивал длинную генерацию (корень «шляпы»). [LLM-watchdog зависшего стрима УЖЕ сделан.]
  3. flushOutbox после server.hello{resumed} + TTL реестра сессий — с полной реализацией resume.

**C. Голос felt-quality** (нужен живой микрофон для приёмки — см. CODE_REVIEW + аудит):
  barge-in `||speech_start` минует порог; общий VAD во время speaking; семантический эндпоинт мёртв; TTS-очередь без watchdog; `createWakeWord` всегда Mock (акустический wake не работает).

**D. Проактивность / живое присутствие** (архитектура, не патч):
  `Sensors` нигде не инстанцируется → `client.context` не шлётся → `salience`/`shouldInterrupt` слепы (только в тестах); importance-кламп; NudgeQueue cap; критический триггер не пробивает locked (будильник молчит); geofence гистерезис.

**E. Точечные остатки аудита** (тюнинг/риск, мелкими заходами):
  web-сервис «где угодно» перебивает намерение (router); валидация dim vs VECTOR(1536) (memory); снимок WorkingMemory для параллельных задач (agent); billing TOCTOU; persona.md (telegram-противоречие, «стоп/отмена» во время shutdown перехватывается control.ts → ПК всё равно гасится).

**F. Документооборот (корень «шляпы»)**: для «реферата/детального» дать агенту контракт ОБЪЁМА (целевой объём/число разделов), а не «Готово» по факту структуры (persona/skill).

**G. НЕ реализуемо без .NET/C#-сайдкара**: десятки input/ground/office/UIA-находок; M7 `order.place` — заглушка.

**Отчёты:** [DIAGNOSIS_2026-06-17_document.md](docs/DIAGNOSIS_2026-06-17_document.md), [CODE_REVIEW_2026-06-17.md](docs/CODE_REVIEW_2026-06-17.md); машинные: `tasks/webuiq7lp.output` (ревью-1), `tasks/w2c9bmmec.output` (анализ голоса).

## 🟢 ИДЕНТИФИКАЦИЯ ГОЛОСА 2026-06-18 — ПОЛ + НАДЁЖНОСТЬ ГЕЙТА (анализ wf_46226c52-2c5, 8 агентов)
**СДЕЛАНО (всё с тестами, зелёное):**
- **Пол М/Ж** — [gender-f0.ts](apps/server/src/voice/speaker/gender-f0.ts): свой YIN/автокорреляция F0 (без зависимостей/GPL), медиана по окнам, voiced-гейт, анти-октава; пороги муж≤155/жен≥165, мягкая зона→unknown; +10 тестов. Подключено в `identify` → `SpeakerMatch.gender/f0Hz` → лог pipeline «верификация диктора {gender,f0Hz}». Пол — ДОП-сигнал, не основной критерий (шёпот/шум→unknown).
- **Фикс self-check эталона** (ревью подтвердило no-op: 0.35<outlier 0.5): порог 0.6 (>floor), итеративный робастный центроид (2 прохода), проверка доли инлайеров (≥0.6) — несогласованная запись (двое/эхо/ТВ) теперь честно отклоняется.
- **Сужена fail-open зона**: REJECT_MARGIN 0.1→0.04 (reject≈0.31 вместо 0.25) — импостор/музыка ~0.3 теперь отсекается, владелец ~0.466 с запасом.

**ОСТАЛОСЬ до «идеала» (по плану ресёрча, нужен живой голос/данные):**
- **AS-Norm + калибровка порога по EER/DET** (P1, наибольший ROI к надёжности): когорта 200-2000 impostor-эмбеддингов (VoxCeleb/CommonVoice-ru/чужие/музыка) → нормировка score → переносимость порога между микрофонами. Сейчас сырой косинус + ручной порог 0.35 — «магическое число под один голос в одной комнате». Нужен сбор данных + офлайн DET.
- **UI-тумблер** «реагировать только на мои голоса» (Фаза 3) вместо env `JARVIS_SPEAKER_GATE` — client+protocol+router+персист.
- **F0-guard в reject/null-ветке** (хранить пол владельца при enroll → отсекать явно чужой пол; НИКОГДА не блокировать в accept-зоне) + многофразовый enroll UI + «слышу: Антон»/«не узнал».
- **Probe-сегментация** (ревью: probe эмбеддится целиком vs центроид из 4с-окон → train/test mismatch занижает score владельца) + единая нормировка enroll/identify.
- Банк из k=5-8 векторов вместо одного центроида (max-cos), гистерезис 2-из-3, окно доверия 8с.
- Полный отчёт анализа: `tasks/w2c9bmmec.output`.

## 🟢 СЕССИЯ 2026-06-17 (вечер, автономная) — СДЕЛАНО (все тесты зелёные: 432 серв. + 71 клиент)
> Подробности: [DIAGNOSIS_2026-06-17_document.md](docs/DIAGNOSIS_2026-06-17_document.md),
> [CODE_REVIEW_2026-06-17.md](docs/CODE_REVIEW_2026-06-17.md). **НЕ закоммичено.**
1. **Разбор «шляпа-документ Пётр I»** (по логам) — корень: рвалась связь с Anthropic (9× стаб
   «связь прервалась» + латентность 6–13с) убивала ход генерации глубокого текста → документ остался
   тонким; Deepgram-WS 174 ошибки/165 пустых финалов → команды не слышались («куча просьб»); фоновое
   YouTube засоряло STT (гейт диктора выкл); wake-gate дропнул «jarrefs». См. DIAGNOSIS.
2. **Адаптивная скорость речи на длинных фразах** (запрос Антона): `adaptiveSpeed()` в
   [voice-providers.ts](apps/server/src/integrations/voice-providers.ts), подключено в Yandex+ElevenLabs;
   env `JARVIS_TTS_SPEEDUP*` (+12 тестов). Короткая фраза — без изменений, длинная — плавно до +12%.
3. **Идентификация голоса — Фазы 0+1+2 СДЕЛАНЫ** (см. ниже, главная задача — обновлён статус).
4. **Многоагентное ревью кода** (28 подтверждённых багов) — применено 10 безопасных фиксов
   (связь/гейт/кеш TTS/память/code_run-внуки/таймауты), остальное с планом в CODE_REVIEW.
5. **UI задач переделан (§20, по фидбэку Антона):** большая панель на весь экран → **угловые
   компактные чипы** (`#taskDock`, по чипу на задачу, видно параллельные разом, у каждого свой
   статус/прогресс/кнопки → правильный taskId). Карточка показывает **суть** (`deriveTaskTitle`),
   а не сырую реплику (`task.title` ⊂ TaskStatus). **Intent edit-vs-new** (`scope.ts`
   `classifyTaskScope`): реплика во время задачи — правка текущей (контекст наследуется) или новая
   отдельная (СВЕЖИЙ контекст, env `JARVIS_TASK_SCOPE`). Файлы: protocol messages.ts, tasks/
   {task,manager,scope}.ts, agent/index.ts, gateway/router-ws.ts; client renderer.ts/index.html/styles.css.
   **Клиент пересобран.** ОСТАЛОСЬ (для истинной правки на лету): очередь правок к РАБОТАЮЩЕЙ петле
   (сейчас правка = новый короткий ход с контекстом, инъекции в живой loop нет).

## 🟢 СВЯЗЬ/НАДЁЖНОСТЬ — СДЕЛАНО 2026-06-18 (reliability-P0 + аудит-P0)
- **LLM-стрим watchdog** ([anthropic.ts](apps/server/src/integrations/anthropic.ts)): нет токенов >25с (`JARVIS_LLM_STREAM_STALL_MS`) → abort+фолбэк (фикс «задача висит в выполняю, панель не закрывается»). + wall-clock потолок задачи 4 мин ([agent/index.ts](apps/server/src/brain/agent/index.ts), `JARVIS_TASK_MAX_MS`).
- **Deepgram reconnect-in-stream** ([deepgram.ts](apps/server/src/integrations/deepgram.ts)): аномальный обрыв (не 1000/auth) → reconnect с backoff (200→2000мс, до 5), `committed` сохраняется, шум ошибок убран; +4 теста (mock-WS).
- **Resume-гонка + двойной heartbeat** ([session.ts](apps/server/src/gateway/session.ts) `isBoundTo`, [server.ts](apps/server/src/gateway/server.ts) close-обработчик): close старого сокета больше не убивает возобновлённую сессию; heartbeat соединения глушится всегда.
- **Аудит-P0 fail-open**: spend_cap (NaN-санитизация+гидрация+config), command-injection в `launchApp` (cmd→Start-Process через ENV), пустой STT-запрос не матчит всех контактов, MockSender fail-closed, episodic NULL-embedding громкий warn, getClient не кеширует reject, taskkill 'error'-handler (краш Electron). Все с тестами.

## 🟢 АУДИТ-ФИКСЫ батчами 2026-06-18 (≈40 P0/P1, все с тестами, зелёные)
- **Клиент-актуаторы:** closeApp wildcard-reject; code-runner hard-resolve+tree-kill; input dedup-модификаторов/handle-validation/typeText-таймаут-по-длине; ground валидация+setValue-guard+screen-mapping; jarvis-browser CDP origin-scope; browser-cdp send() try/catch+onWsDown kill; fs search-cap-в-цикле.
- **Задачи:** manager.progress кламп, active() тай-брейк `>=`; control TASK_STEMS чистка + фразы по границам; scope «короче»/ссылки по границам; narrate «Шаг N из M».
- **Сообщения/§14:** contacts пустой-запрос fail-closed; MockSender fail-closed; cadence channel-в-ключе; outbound применяет suggestedDelayMs (анти-бан); order-guard card-separator расширен (Luhn).
- **Персист (атомарно tmp→rename + сериализация):** profile.ts, consent.ts (+ ранее store voices.json); persona/index не кеширует фоллбэк.
- **NLU:** modes по границам токенов (убраны «включи/говори»); sentences десятичная дробь через дельты.
- **Связь/деньги (ранее этой сессии):** Deepgram reconnect, resume-гонка, LLM-watchdog, spend_cap NaN+гидрация, command-injection, getClient reject-cache, taskkill-краш, episodic NULL-warn.

## 🔴 ОСТАЛОСЬ ИЗ АУДИТА (следующие батчи)
- ✅ **router** — fuzzy-склонения (убрана «почта», guard падежных форм), СДЕЛАНО. (Веб-сервис «где угодно» перебивает намерение — осталось, тюнинг-чувствительно.)
- ✅ **memory/skills** — saveSkill fail-open при ошибке БД теперь громкий warn, СДЕЛАНО. (Валидация dim vs VECTOR(1536), version-бамп — осталось.)
- ✅ **agent** — стрим-потом-«не смог» (флаг spokeAny), СДЕЛАНО. (Общий WorkingMemory снимок — осталось, риск.)
- ✅ **billing** — месячный rollover spent, СДЕЛАНО. (TOCTOU параллельных задач — осталось, нужен reserve-механизм.)
- **proactive** — оживить контур (Sensors не инстанцируется, salience мёртв), importance-кламп, NudgeQueue cap, geofence гистерезис.
- **persona.md** — противоречие про telegram_read; «стоп/отмена» во время shutdown перехватывается control.ts (ПК всё равно гасится).
- **Голос (нужен живой микрофон):** barge-in `||speech_start` минует порог; общий VAD во время speaking; семантический эндпоинт; TTS-очередь watchdog; createWakeWord всегда Mock.
- **Не реализуемо без C#-сайдкара/.NET:** десятки input/ground/office/sidecar находок; M7 order.place заглушка.

## 🔴 ОСТАЛОСЬ ПО СВЯЗИ/НАДЁЖНОСТИ
- **Persistent Deepgram WS на разговор** (не переоткрывать каждый ход — churn ~0.3-0.5с/ход).
- **flushOutbox после server.hello{resumed}** (клиент): сейчас результаты теряются при resumed=false — чинить вместе с полной реализацией resume.
- **TTL реестра сессий** — пока покрыт heartbeat→close→remove; добавить defensive sweep при необходимости.
- **Устойчивость к таймаутам Anthropic на ТЯЖЁЛОМ ходе** — стрим/больший max_tokens, чтобы стаб не убивал длинную генерацию (корень «шляпы»).

## 🎯 СЛЕДУЮЩАЯ ГЛАВНАЯ ЗАДАЧА (запрос Антона 2026-06-17): НАДЁЖНАЯ ИДЕНТИФИКАЦИЯ ГОЛОСА
> Грунтовано многоагентным ресёрчем `wf_cba61047-9ef` (7 агентов: карта кода + ресёрч gender/SV + синтез).
> Полный отчёт с цитатами кода и источниками: task-output `tasks/waph4cwgn.output`.

**Три связанные цели пользователя:**
1. **Идентификация голоса** — узнавать КТО говорит (владелец vs известные имена, напр. Катя, vs чужой), сделать НАДЁЖНОЙ.
2. **Различать мужской/женский голос.**
3. **Фича «реагировать ТОЛЬКО на сохранённые голоса» (`JARVIS_SPEAKER_GATE`) должна РЕАЛЬНО работать** — без ложного глушения владельца и без пропуска чужих.

**ПОЧЕМУ СЕЙЧАС НЕ РАБОТАЕТ (корни, подтверждены кодом):**
- **Гейт де-факто мёртв по умолчанию** — тройное условие: `JARVIS_SPEAKER_GATE==='1'` ([router-ws.ts:189](apps/server/src/gateway/router-ws.ts)) И `verifier.ready` И `profiles().length>0` ([pipeline.ts:311](apps/server/src/voice/pipeline.ts)). Флаг не выставлен → `speaker===undefined` → реагируем на всех. Hint в UI («отвечает ТОЛЬКО на записанные голоса», [index.html:147](apps/client/renderer/index.html)) — ложь без флага. Состояние зашито в env, тумблера в меню нет.
- **Хрупкий эталон** — `enroll().finish()` ([sherpa-verifier.ts:114](apps/server/src/voice/speaker/sherpa-verifier.ts)) склеивает ВСЮ речь в ОДИН эмбеддинг (один микрофон/интонация/дистанция); `store.add` перезаписывает профиль целиком, банка нет.
- **Узкий запас по порогу** — `THRESHOLD=0.35` при score владельца **~0.466** ([sherpa-verifier.ts:40-45](apps/server/src/voice/speaker/sherpa-verifier.ts)): полоса ~0.11. Простуда/смена мик/шум роняют score к порогу → ложный реджект. Нет score-нормализации (AS-Norm) → порог плавает между каналами.
- **Двойной fail-open пускает чужого** — `checkSpeaker` → `true` при `identify===null` ([pipeline.ts:323](apps/server/src/voice/pipeline.ts)) и при любом throw ([pipeline.ts:329](apps/server/src/voice/pipeline.ts)); `identify` сам отдаёт null на тихом/коротком ходе (`IDENTIFY_MIN_SEC=0.4с`/`IDENTIFY_MIN_PEAK=0.06`, [sherpa-verifier.ts:133](apps/server/src/voice/speaker/sherpa-verifier.ts)). Короткие/тихие команды («стоп», «да», шёпот) и удалённый чужой обходят биометрию.
- **Вердикт ход-в-ход без гистерезиса** ([pipeline.ts:324](apps/server/src/voice/pipeline.ts)) → пограничный score флапает свой↔чужой.
- **Нет обратной связи «не узнал»** — `gateWake` возвращает `''` и только пишет в лог ([pipeline.ts:207](apps/server/src/voice/pipeline.ts)); владелец видит молчащего Джарвиса без причины.

**ПЛАН по убыванию ROI (минимум надёжности = Фаза 1 + Фаза 2):**

- **✅ Фаза 0 — СДЕЛАНО (2026-06-17):** `dim`+`modelId` в `VoiceProfile`/`StoredVoice` ([store.ts](apps/server/src/voice/speaker/store.ts)); `identify` отбраковывает профиль чужой dim/modelId (`isComparable`) → конец тихой деградации `cosine` по min(len) ([sherpa-verifier.ts](apps/server/src/voice/speaker/sherpa-verifier.ts)); legacy-профиль Антона совместим (dim выводится из длины). Профиль при enroll тегируется моделью (router-ws). **Осталось:** офлайн CLI-калибровка `calibrate.ts` (target/impostor → EER → порог); лог сырых score за env-флагом.
- **✅ Фаза 1 — СДЕЛАНО (2026-06-17):** `enroll()` переписан на multi-sample центроид ([sherpa-verifier.ts](apps/server/src/voice/speaker/sherpa-verifier.ts) `buildCentroid`) — фикс-окна 4с РЕЧИ (env `JARVIS_ENROLL_WINDOW_SEC`), эмбеддинг+L2-норм каждого, отбраковка выбросов (`ENROLL_OUTLIER_FLOOR`), усреднение → центроид (тот же blob); **self-check** перед сохранением (средний косинус окон ≥ `ENROLL_SELFCHECK_MIN`, иначе null) — закрывает «мусорный эталон глушит». +5 тестов. **Осталось:** многофразовый UI «Фраза 2 из 4» (`VoiceEnrollProgress`).
- **✅ Фаза 2 (пороги) — СДЕЛАНО (2026-06-17):** `acceptThreshold`/`rejectThreshold` в [verifier.ts](apps/server/src/voice/speaker/verifier.ts) (reject = accept − `JARVIS_SPEAKER_REJECT_MARGIN`, деф 0.10); гейт [pipeline.ts](apps/server/src/voice/pipeline.ts) `checkSpeaker` — зона `[reject,accept)` → fail-open (просадка score больше не запирает владельца), глушим только `< reject`. Обратная совместимость: Mock accept=reject=threshold. **Осталось (temporal smoothing):** кольцевой буфер 2-3 вердиктов / окно доверия от уверенного «свой» — НЕ сделано (конфликтует с текущей семантикой одного вердикта в тестах гейта; нужен редизайн gate-тестов).
- **Фаза 3 — UI-тумблер + честность:** переключатель «Реагировать только на мои голоса» в меню «Голоса», персист состояния, новая команда `voice.gate.set`; гейт по тумблеру, НЕ по env (env остаётся аварийным force-off). Событие `voice.speaker {name?, score, gated}` → орб «не узнал голос»/«слышу: Антон». Hint зависит от состояния.
- **Фаза 4 — gender (цель №2) + закрытие fail-open:** MVP — **свой чистый JS F0** (автокорреляция/YIN, НЕ `pitchfinder` — GPL-v3) в `gender-f0.ts` над уже собираемым `turnAudio`, без новых зависимостей/сети. Хранить pitch-статистику владельца (mean/std) при enroll; на identify — если pitch пробы радикально вне диапазона ВСЕХ профилей, глушить чужого ДАЖЕ в null-ветке (guard, не основной критерий). Метка М/Ж (~165 Гц, мягкая зона 150-180) в орб. Опц. позже: ONNX wav2vec2 gender через УЖЕ присутствующий `onnxruntime-node` (транзитив sherpa-onnx-node), модель prithivMLmods Apache-2.0.
- **Фаза 5 — AS-Norm + EER-калибровка (структурный инкремент, опц.):** когорта 20-50 чужих эмбеддингов (`cohort.bin`, офлайн через тот же CAM++) → `asNorm()` делает порог переносимым между микрофонами; перекалибровка accept/reject по EER ПОСЛЕ нормировки. Опц. адаптивное дообучение центроида (только near-perfect ходы + анти-абуз).

**ГРАБЛИ/риски (НЕ переоткрывать):**
- **Миграция `voices.json`** — у Антона уже есть профиль (откалиброван 2026-06-17). Старый blob без `dim`/центроида: дефолтить `dim=192`/`modelId='campp-3dspeaker-192'`, трактовать как центроид `count=1`. Тестировать на КОПИИ файла.
- **Включение гейта по умолчанию повторит регресс «оглох»** (из-за него и отключили, [router-ws.ts:186](apps/server/src/gateway/router-ws.ts)). Митигация: тумблер ВКЛ предлагать ТОЛЬКО после self-check Фазы 1; env force-off оставить; событие «не узнал» = быстрый сигнал регресса.
- **F0 шумен** на шёпоте/напеве/коротком — только guard к null-ветке, `voiced=false` → не классифицировать; межполового импостора в том же диапазоне не ловит.
- **Кириллица в пути** ломает нативную загрузку sherpa — модель/когорта ТОЛЬКО в ASCII-пути (`~/.jarvis/models/`).
- Anti-spoofing (реплей/ТВ) — НЕ делать сейчас: другой класс задач, не корень; музыка/ТВ и так дают низкий косинус.

**Открытые вопросы (решить по ходу):** где персистить состояние тумблера (поле в voices.json / общий конфиг); сколько target/impostor записей реально доступно у Антона для EER (сейчас единственная точка — score 0.466); сегментация enroll по паузам vs фикс-окна (предложено фикс-окна); банк — только центроид vs +1-2 разнесённых вектора; live-индикация опознания на каждом ходе или только при reject; включать ли адаптивное дообучение вообще (риск отравления).

**Текущее покрытие тестами (что ДОБАВИТЬ):** нет теста на различение М/Ж, нет калибровки порога/EER, нет усреднения нескольких сэмплов, нет гистерезиса/анти-флапа, нет интеграционного теста гейта «свой vs чужой vs музыка» в pipeline (всё юнитовое на fakeExtractor). См. `tasks/waph4cwgn.output` для списка.

---

## ✅ ФИКС 2026-06-17: «записал голос → Джарвис оглох» + тихий микрофон (388 серв. + 71 клиент тестов зелёные)
Жалоба: записал свой голос в «Голоса» → Джарвис перестал слышать. Корень (2 связанных бага):
- **A. Enrollment без проверки речи** ([sherpa-verifier.ts](apps/server/src/voice/speaker/sherpa-verifier.ts)):
  прогресс шёл по ЛЮБОМУ аудио (`total/SR/enrollSeconds`) → 20с эха/тишины = «успех» = мусорный эталон →
  гейт диктора глушил даже владельца. Триггер ручной (кнопка «Записать голос»); авто-enroll в коде НЕТ.
- **B. Тихий микрофон** (`peak 0.01–0.04` даже на речи): верификатор снимал отпечаток с почти-тишины
  (Deepgram-то тянул своим `StreamAgc`, а верификатор — нет). Это же давало фоновые пустые транскрипты.

Фиксы (env-тюнятся, +8 тестов `sherpa-verifier.test.ts`):
1. enroll считает прогресс ТОЛЬКО по речи (пик ≥ `JARVIS_ENROLL_SPEECH_PEAK`=0.12); тишина шкалу не растит.
2. `finish()` → null при речи < `JARVIS_ENROLL_MIN_SPEECH_SEC`=6с → честное «не удалось» (UID уже показывает).
3. enroll+identify нормируют пик (`normalizePeak`→0.3) → косинус сравнивает голоса, не громкости.
4. `identify()` → null на тихом(`<JARVIS_IDENTIFY_MIN_PEAK`=0.06)/коротком(<0.4с) ходе → гейт ПУСКАЕТ
   (fail-open: enrolled-голос больше НЕ глушит владельца на тихом обороте). `enrollSeconds` 20→12 (сек речи).
5. **makeup-gain** ([renderer/audio.ts](apps/client/renderer/audio.ts)): фикс. tanh-`WaveShaperNode` (k=6) между
   source и ворклетом — тихий микрофон ×6 почти линейно, громкий мягко насыщается <1 (без клиппинга ±1). НЕ AGC.
- **Трейдофф (намеренно):** fail-open на тихом ходе = тихий чужой/ТВ может проскочить — выбрано против оглушения владельца.
- **Чистка мусорного отпечатка:** `rm apps/server/data/voices.json` + рестарт сервера (in-memory store → `voices:0`, гейт выкл).
- **⚠️ На СЛУХ НЕ проверено** (нужен живой голос): записать голос внятно → шкала должна заполняться только на речи;
  если тихо — «не удалось»; после успешной записи Джарвис должен слышать ТЕБЯ и игнорить музыку/чужих.

### Живой тест 2026-06-17 (на слух) + ещё 2 фикса
- **Калибровка порога:** makeup-gain поднял уровень (пик 0.8, транскрипт идеальный), enrollment снялся на речи. НО
  собственный голос дал косинус **0.466** при пороге 0.5 → гейт резал владельца. ФИКС: дефолт `THRESHOLD` 0.5→**0.35**
  ([sherpa-verifier.ts](apps/server/src/voice/speaker/sherpa-verifier.ts)). Лог-строка `верификация диктора { score, ok }`.
- **Протечка гейта (ФИКС, +1 тест):** чужой (`score −0.027`) отклонялся, НО его реальный финал Deepgram доходил до
  агента (спекулятивный путь резался, поздний финал — нет: `speakerRejected` сбрасывался ensureStt до прихода финала).
  ФИКС: постримный closure-флаг `streamSpeakerRejected` в [pipeline.ts](apps/server/src/voice/pipeline.ts) (`rejectActiveStream`).
- **Само-восстановление прослушки (ФИКС, +1 тест):** Джарвис «глох» после пустого/чужого хода И после enrollment —
  эндпоинт без транскрипта → `close_stt`, состояние осталось «listening», но STT закрыт; `onAudioFrame` переоткрывал
  только из `idle`. ФИКС: `if (state==="listening" && !sttStream) ensureStt()` в [pipeline.ts](apps/server/src/voice/pipeline.ts) —
  переоткрытие на следующей речи. Закрыло и «глохнет после пустого хода», и «не слышит сразу после записи голоса».

## ЛАТЕНТНОСТЬ: ускорение Opus-пути (решение пользователя «только Opus, выжать накладные»)
Пользователь хочет обычный диалог ≤1с. Выбор: НЕ переходить на Haiku (всегда Opus), жать накладные.
Реалистично ~1.3–1.7с (на тёплых ходах ближе к низу), НЕ гарантированно ≤1с — пользователь это принял.
Сделано (362 теста зелёные):
- **Спекулятивный старт STT** ([pipeline.ts](apps/server/src/voice/pipeline.ts) `endpointTurn`) — на эндпоинте,
  если interim-текст уже накоплен, дёргаем агента СРАЗУ, не ждём CloseStream+flush Deepgram (~200мс). Поздний
  финал стрима игнорируется (state=thinking). Оба пути эндпоинта (VAD speech_end + silence-poll tick) → endpointTurn.
- **Тёплый кэш с 1-го хода** ([agent/index.ts](apps/server/src/brain/agent/index.ts) `cachePrefix=true` всегда) —
  Opus не перечитывает огромный префикс (персона+инструменты) каждый ход → меньше TTFT со 2-го хода. Цена: 1.25× запись 1 раз.
- **Жёсткий бюджет retrieval на голосе** — `ioTimeoutMs = sink ? 350 : 2000` (память не держит первый токен; модель добёрет memory_search).
- **Сложная задача → мгновенный ack** уже есть (sonnet/fable → `startBackgroundTask`+`ackReply`, работа в фоне);
  расширил пул acks («Секунду, разберусь.», «Сейчас гляну.»). Срабатывает, когда classifyTier даёт sonnet/fable.
- Замер: лог-строка `latency: фраза→LLM … · LLM→TTS … · →звук …` — читать после живых ходов.
- Открытый рычаг (если ≤1с критично): спекулятивно стартовать LLM ещё до конца фразы; или Haiku на лёгком пути (отклонено пользователем).

## РЕЧЕВАЯ БИОМЕТРИЯ (kill-фича) — реагировать только на голос владельца — ШАГ 1 СДЕЛАН
Запрос: узнавать голос пользователя (+ доп. голоса с именами, напр. Катя), игнорить музыку/YouTube/чужих.
Дизайн: фильтр на «реагировать ли», НЕ «слушать ли»; отпечаток считается ПАРАЛЛЕЛЬНО на уже текущем аудио →
вердикт к концу фразы → НОЛЬ добавленной задержки; меню «Голоса» с enrollment (~полминуты, прогресс %);
короткие реплики при нехватке аудио — пропускать (не запирать). Гейт ВЫКЛ, пока нет enrolled-голоса (безопасно).
**✅ ШАГ 1 — ядро (8 тестов):** `verifier.ts` (`ISpeakerVerifier` enroll/identify, `MockSpeakerVerifier`,
`VoiceProfile`) + `store.ts` (`VoiceProfileStore` → data/voices.json, имя+base64-отпечаток, add/remove/list/hasAny).
**✅ ШАГ 3 — ДВИЖОК (готов, KEYLESS, ПРОВЕРЕН на машине):** Picovoice отпал — требует КОРПОРАТИВНУЮ почту (gmail
не пускает). Сделано на **sherpa-onnx-node** (`pnpm` поставлен, win-x64 бинарь, мел-фронтенд внутри) + модель
**CAM++ 3dspeaker zh+en 16k (192-dim, 27МБ)**. Адаптер `sherpa-verifier.ts` (`createSpeakerVerifier` factory:
есть модель+либа → реальный, иначе Mock=гейт выкл). Смоук: enroll→identify, свой score=1.0 — РАБОТАЕТ.
  - **ГРАБЛИ (важно):** нативный загрузчик sherpa НЕ читает модель из пути с КИРИЛЛИЦЕЙ (проект под `…/Автокомп/…`).
    Модель ОБЯЗАНА лежать в ASCII-пути → дефолт `~/.jarvis/models/speaker-embedding.onnx` (туда уже скопирована).
    Env override `JARVIS_SPEAKER_MODEL` (тоже ASCII). Порог `JARVIS_SPEAKER_THRESHOLD` (деф 0.5, откалибровать на живых голосах).
  - На сервере (клиент тонкий/телефон не нагружены), CPU, ~копейки; считается параллельно → 0 задержки.
**✅ ШАГ 2 — ГЕЙТ (готов, 2 теста):** [pipeline.ts](apps/server/src/voice/pipeline.ts) — буфер аудио хода
(`turnAudio`, кап 8с) → на эндпоинте `checkSpeaker()` опознаёт диктора ПАРАЛЛЕЛЬНО → `gateWake` глушит чужого
(пустой транскрипт = игнор). Свой score≥threshold → пускаем; чужой → дроп; короткий/тихий (identify=null) →
пропускаем (не запираем). Гейт активен только при enrolled-голосах. Вердикт `identify` отдаёт лучший
match+score (порог применяет гейт). Обвязка: router-ws (`speakerVerifier`/`speakerStore` на gateway → pipeline),
server.ts (init на старте). Тесты: свой→обработан, чужой/музыка→дроп.
**✅ ШАГ 4 — ENROLLMENT (готов):** реюз аудиопотока (audio.frame) — НЕ новый канал. Protocol:
`voice.enroll.start/cancel`, `voice.list`, `voice.remove` (c→s); `voice.enroll.progress/done`, `voice.voices` (s→c).
Сервер (router-ws): `ctx.enroll` сессия; во время записи audio.frame кормит `EnrollSession` (прогресс с троттлом),
на 100% → `store.add` → done + список. Клиент: transport-методы, IPC, preload, **вкладка меню «Голоса»**
([index.html](apps/client/renderer/index.html)+[renderer.ts](apps/client/renderer/renderer.ts)): имя + «Записать голос» +
прогресс + список с удалением. Перезапущено, 443 теста зелёные, boot чистый (движок ready, гейт off при 0 голосах).
**ФИЧА ГОТОВА end-to-end.** На СЛУХ проверить: записать свой голос (Настройки→Голоса), убедиться что музыка/чужой
игнорятся, а свой слышен; добавить Катю. Если ложно режет/пускает — крутить `JARVIS_SPEAKER_THRESHOLD` (деф 0.5).

## РЕАЛЬНОЕ УПРАВЛЕНИЕ WINDOWS — ОТКРЫТО (запрос пользователя; persona v19, 433 теста зелёные)
Философия пользователя: «реальные инструменты как у тебя и больше; НЕ прописывай шаблоны как делать —
он сам». Решение: безопасность из экспертизы агента, НЕ из блок-листов. **Снято:** блок-листы сети/
реестра/служб/системных путей/shell-exec/обфускации в [code-guard.ts](apps/server/src/brain/code-guard.ts);
PowerShell **Constrained Language Mode → FullLanguage** в [code-runner.ts](apps/client/main/actuators/code-runner.ts)
(Add-Type/COM теперь работают — этим и переключают аудио и т.п.); env раннера = реальный (минус секреты
*KEY/TOKEN/…); таймаут 10с→30с (env `JARVIS_CODE_TIMEOUT_MS`, кламп 5-180с); `python -c` (без `-I`);
action-timeout code.run → 185с. Схема `code_run` и персона §6 переписаны: «код = твои руки, делай САМ, не запасной путь».
**ОСТАВЛЕНЫ только КРИТИЧНЫЕ РЕЛЬСЫ §4** (выбор пользователя — «критичные рельсы»):
1. Самозащита — нельзя kill electron/node/sidecar по имени (другие процессы/PID — свободно).
2. Питание — только `system_power` (отложенно, с предупреждением/отменой); shutdown в code.run заблокирован.
3. Необратимое — удаление файлов/формат диска → confirm (не блок).
4. Карты/платёжные данные — красная линия §0 (отдельно).
**НЕ строить спец-инструменты под задачи и НЕ прописывать рецепты** — общие руки + автономия.
Риск принят пользователем (его машина). Откат рельсов до «максимум как у тебя» — убрать confirm на удаление.

## ПАМЯТЬ: «вспоминает странные графики на старте» — ИСПРАВЛЕНО (persona v18)
Жалоба: на старте Джарвис «вспоминал» то, чего пользователь не говорил. Корень: приветствие
([greeting.ts](apps/server/src/proactive/greeting.ts)) делало generic векторный поиск по эпизодам
(«недавние дела…», топ-3, БЕЗ порога релевантности) и вшивало результат в опенер; а агент
АВТО-писал КАЖДУЮ реплику >3 симв. как `event` (STT-мусор/команды/обрывки) → потом всплывало. Фиксы:
- **Приветствие больше НЕ тянет сырые эпизоды** — только время + имя + курируемые факты профиля. Чистый старт.
- **Убрана авто-запись каждой реплики** ([agent/index.ts](apps/server/src/brain/agent/index.ts)) — память теперь ОСОЗНАННАЯ.
- **Персона §8 «Память — осознанная»**: `memory_write` только значимое/долговечное (образ жизни/привычки/
  предпочтения), НЕ болтовню/команды/мусор; `memory_search` — только когда релевантно.
- **ОСТАЁТСЯ**: старый авто-мусор всё ещё в pgvector (`episodic_memory`, kind='event'). На старте не всплывает,
  но при memory_search может. ОЧИСТКА (по запросу пользователя): `delete from episodic_memory where kind='event';`
  (сохранит осознанные kind='fact'/'preference'). Не сделано — деструктивно, нужно согласие.
- Нет связки memory_write→profile.facts: greeting тянет facts из профиля (getProfile().facts), а memory_write
  пишет в эпизод. Чтобы приветствие проактивно упоминало образ жизни — нужен курируемый слой фактов (TODO).

## 🎯 СЛЕДУЮЩАЯ ЗАДАЧА (запрос пользователя): ЖИВАЯ РЕЧЬ — план в [HUMANIZE_VOICE.md](docs/HUMANIZE_VOICE.md)
Сделать произношение человечным (паузы/ритм/интонация) через то, что шлём в ElevenLabs. Детальный план + файлы + грабли — в HUMANIZE_VOICE.md.

## ЗАЩИТА ОТ ВЫКЛЮЧЕНИЯ БЕЗ ПРЕДУПРЕЖДЕНИЯ (запрос пользователя) — СДЕЛАНО
Многослойно (438 тестов зелёные, persona v17):
- **Отложенный shutdown/restart** ([system.ts](apps/client/main/actuators/system.ts)) — `shutdown /s /t N /c "…"`
  вместо `/t 0`: ОС показывает предупреждение + окно отмены. N = env `JARVIS_SHUTDOWN_DELAY_SEC` (деф. 25с, кламп [5,600]).
- **Новый op `cancel`** (PowerOp в [actions.ts](packages/protocol/src/actions.ts), схема, актуатор `shutdown /a`,
  `tolerateFailure`) — Джарвис отменяет запланированное по «отмена/стоп»; без confirm.
- **confirm-гейт** ([dispatch.ts](apps/server/src/brain/tools/dispatch.ts)) на shutdown/restart/logoff остался; sleep/cancel — без confirm.
- **code_run БОЛЬШЕ НЕ может выключить ПК** ([code-guard.ts](apps/server/src/brain/code-guard.ts)) — `power`-правило теперь
  БЛОК (не confirm), во всех языках: питание только через system_power (защищённый путь).
- **Персона** ([persona.md](apps/server/src/brain/persona/persona.md)) — обязан озвучить «выключаю через N сек, скажите отмена» и звать `cancel`.

## БАГ −2 (из реального диалога) — Джарвис барахтался и злил
Лог: «переключи динамики на Razer» → `code_run`/`ui_ground`/`screen_capture` ошибки → юзер «да заебал». Причины+фиксы:
- `screen_capture` был заглушкой (notImplemented), но предлагался модели → **убран из набора** (EXCLUDED_TOOLS).
- **Реплики Джарвиса не логировались** → добавлен лог `Джарвис →` (диалог теперь читаем для диагностики).
- **Персона v16**: раздел «Живой разговор» — реагировать на настроение, держать нить, деградировать ИЗЯЩНО, уточнять вместо «угадал-провалился».
- **Пробел**: нет инструмента смены аудиоустройства (только громкость) → «переключи на Razer» нечем сделать. КАНДИДАТ на отдельный инструмент.

## Латентность (намерено; лог-строка `latency:` добавлена)
Первый звук ~3.2–4.6с: фраза→первый-токен Opus ~2.2–3.6с (доминирует) + TTS ~1с (не стримится). Рычаги: спекулятивный старт STT, TTS-стриминг (нужна правка клиента-плеера), persistent WS. НЕ сделано.

---

## ⚠️ ПЕРВЫМ ДЕЛОМ: проверить голос НА СЛУХ (длинный разговор, БЕЗ повторного «Джарвис»)
Внедрён фикс **«слушает 5-10с и опять перестаёт»**. Живая проверка: скажи «Джарвис, …», дождись
ответа, дальше веди ОБЫЧНЫЙ диалог НЕ повторяя «Джарвис» — пауза 10-20-40с между репликами не
должна глушить (раньше глох через 12с). Перед стартом: `pnpm --filter @jarvis/client build` уже
сделан; просто перезапусти клиент. Сервер под `tsx watch` подхватил правки сам.

## БАГ 0 — «слушает 5-10с и перестаёт» — ИСПРАВЛЕНО (ждёт проверки на слух)
**Корень (по логам server.log/client.out.log + коду):** НЕ AGC и НЕ клиентский гейт (гейт открыт
постоянно, кадры идут до 25000+). Корень — серверный gate разговора: `requireWakeWord:true` +
окно разговора **12с**, привязанное ТОЛЬКО к моменту речи Джарвиса. Через ~12с после его ответа
любая фраза без слова «Джарвис» молча отбрасывалась (`gateWake` → «реплика без обращения — игнор»),
а реального wake-детектора нет (MockWakeWord). Плюс follow-up 6с (орб мигал в idle) и дребезг VAD.

**Фикс (4 точечных правки, 449 тестов зелёные):**
1. [pipeline.ts](apps/server/src/voice/pipeline.ts) `gateWake` — окно разговора КАТИТСЯ от КАЖДОЙ
   принятой реплики (поле `lastSpokeAt`→`lastActiveAt`, обновляется и на речь Джарвиса, и на
   принятый ход юзера). Диалог не глохнет посреди разговора.
2. [router-ws.ts](apps/server/src/gateway/router-ws.ts) — окна щедрые и env-тюнятся:
   `conversationWindowMs=180_000` (env `JARVIS_CONV_WINDOW_MS`), `followupMs=30_000`
   (env `JARVIS_FOLLOWUP_MS`). Орб держит «listening», STT живёт на KeepAlive.
3. [vad/index.ts](apps/client/main/vad/index.ts) — онсет-дебаунс `onsetFrames=3`: speech_start
   только после устойчивой речи → конец дребезга speech_start/end на шуме (флудил turn-detector).
4. [transport/index.ts](apps/client/main/transport/index.ts) — кап реконнект-backoff 5с (было 30с):
   клиент встаёт ≤5с после возврата сервера, а не через полминуты (старт-шторм ECONNREFUSED).

**Wake-word «Джарвис» теперь нужен только чтобы НАЧАТЬ разговор** (или после долгой паузы > окна).

## БАГ −1 — «Джарвис тупой vs Claude» — ЧАСТИЧНО ИСПРАВЛЕНО
**Жалоба:** «он тупой, хотя тот же Opus через Anthropic API». **Модель НЕ виновата** — в `.env` все
тиры = `claude-opus-4-8`; маршрут в haiku-тир НЕ понижает модель. **Корень — персона** душила интеллект:
приказ «3–7 слов на ВСЁ» → на содержательный вопрос Opus давал огрызок. **ФИКС: persona v14→v15**
([persona.md](apps/server/src/brain/persona/persona.md)) — раздел «Тон и длина ответа ПО СИТУАЦИИ»
(команда→кратко, вопрос→содержательно/умно 2–5 фраз, детали→карточка) + характер JARVIS. 51 brain-тест зелён.
**Остаток (в фоновом ревью):** extended thinking Opus (вкл/нет, ценой латентности), мусор от STT, путь самопроверки.
Подробности — в памяти `project_jarvis_intelligence.md`.

## ГЛУБОКОЕ РЕВЬЮ ЗАВЕРШЕНО + ПАТЧИ ПРИМЕНЕНЫ (wf_3d6ef1ef-d62, 25 агентов, состязат. проверка)
Полный отчёт: `subagents/workflows/wf_3d6ef1ef-d62/` (или task-output `tasks/w1cl6bssx.output`).

**ПРИМЕНЕНО этой сессией (435 тестов зелёные, клиент пересобран):**
- **#1 max_tokens 1024→4096** ([anthropic.ts](apps/server/src/integrations/anthropic.ts)) — Opus обрывался на полуслове
  (ответы И вывод инструментов: код/документы). Крупнейший вклад в «тупость» после persona. stop_reason=max_tokens теперь редкость.
- **#4 CadenceGuard.history** ([cadence.ts](apps/server/src/brain/messaging/cadence.ts)) — вытеснение по окну (была утечка/O(n)).
- **#6 anti-echo barge-in grace 350мс** ([audio/index.ts](apps/client/main/audio/index.ts)) — не рубим TTS на эхо-хвосте своего же голоса (+тесты, инъекция часов).
- **#7 семантический эндпоинт ОЖИЛ** ([turn.ts](apps/server/src/voice/turn.ts)+[pipeline.ts](apps/server/src/voice/pipeline.ts)) — был один таймер на 800мс → ранний эндпоинт не срабатывал НИКОГДА; теперь опрос tick() от minSilence → завершённая фраза отвечает быстрее.
- **#8 selfLearnSkill на haiku-тире** ([agent/index.ts](apps/server/src/brain/agent/index.ts)) — рефлексия не жжёт топ-тир.
- **#9 retrieval/recall таймаут 2000→800мс на голосовом пути** (agent/index.ts) — меньше мёртвой паузы перед 1-м токеном.
- **#10 mergeStutters near-equality** ([disfluency.ts](apps/server/src/brain/nlu/disfluency.ts)) — «план планировщик» больше не съедается (дефис-обрыв ловим даже с пунктуацией).

**ВАЖНЫЕ ПОПРАВКИ ревью (НЕ делать наивно):**
- ❌ **Extended thinking** — `thinking:{type:'enabled',budget_tokens}` даёт HTTP 400 на fable-5/sonnet-4-6 (и `{disabled}` на Fable 5).
  Только `{type:'adaptive'}` без budget, не на haiku/синхронном ходе, ПОСЛЕ max_tokens-фикса. Сверять с актуальным API. НЕ внедрено.
- ❌ **Дизайн — НЕ заменять styles.css целиком.** По сути миграция УЖЕ выполнена (палитра/токены/кнопки совпадают байт-в-байт).
  Прямая замена сломала бы: `.hero__bar`+`@keyframes eq` (визуализатор-бары), `.settab*` (вкладки настроек), пару @font-face Manrope (latin+cyrillic).
  `ПЕРЕНОС.md` устарел (canvas/bg). Брать из импорта — только пофрагментным мёржем дельт. Импорт лежит в `docs/_design_import/`.
- ⚠️ Дефолт моделей в коде РАЗНЫЙ (haiku-4-5/sonnet-4-6/fable-5), но у пользователя `.env` = всё Opus.

**ОТЛОЖЕНО (крупные рефакторы, отдельными заходами):**
- **#2 TTS-стриминг** — НО клиент `AudioPlayback` буферит до `last=true` (renderer/audio.ts): серверный стрим без правки клиента НЕ ускорит. Нужны обе стороны (или flash на 1-ю фразу под флагом — риск тембра).
- **#3 спекулятивный старт агента** (~200мс) — диспатчить transcript_final сразу через gateWake, close+flush в фон.
- **Persistent Deepgram WS** на разговор (~0.1-0.4с/ход); **AbortController** через синхронный ход (barge-in рвёт Opus, не дожигает); **vision+voice запись навыков** (нужен screen.capture+image-блок в LlmContentBlock — сейчас demo.record/screen.capture = notImplemented); **SpendGuard reserve-then-reconcile**; очередь семафора видимой для отмены; **AsyncMutex.release идемпотентным** (двойной release ломает аренду ввода).

---

## БАГ 1 — «слышит один раз и глохнет» — ИСПРАВЛЕНО (ждёт проверки)
**Корень (подтверждён 4 диагностами ревью + ручным разбором кода):** `autoGainControl`
WebRTC-цепочки душит усиление микрофона под громкое эхо ПЕРВОГО ответа Джарвиса (мик горячий
ради barge-in), а в тихой комнате после TTS усиление НЕ восстанавливает → 2-я фраза приходит
на peak ~0.25 → Deepgram отдаёт пустой транскрипт. Рестарт `getUserMedia` сбрасывал AGC —
поэтому «один раз слышал». Доказательства в логах: turn-1 peak `0.979`+финал, сразу после
TTS turn-2 peak `0.250`, VAD-дребезг `speech_start/end` каждый кадр, гейт открыт, кадры летят.

**Фикс:** [audio.ts](apps/client/renderer/audio.ts) — `getUserMedia({ echoCancellation: true,
noiseSuppression: false, autoGainControl: false })`. AEC ОСТАВЛЕН (нужен против barge-in на
собственный TTS). NS+AGC выкл — APM деформирует сигнал, облачный STT шумоподавляет сам.
Также ранее добавлен mic-резилиенс (track `onmute/onended` → реинит `getUserMedia`) — ловит
потерю аудиоустройства (Dota и т.п.), но НЕ AGC-коллапс (трек не мьютится) — потому корень в AGC.

**Если raw-уровень окажется низким** (мик тихий без AGC-буста): добавить ФИКСИРОВАННЫЙ
makeup-`GainNode` (не адаптивный!) в граф захвата `start()`, либо поднять уровень мика в Windows.
Откат: вернуть `autoGainControl: true` (но тогда баг вернётся).

## БАГ 2 — создание скилла «нихуя не сохраняет» — НЕ ИСПРАВЛЕНО (приоритет след. сессии)
**Корень:** запись демонстрации завязана на **C#-сайдкар** [DemoRecorder.cs](apps/sidecar-win/DemoRecorder.cs)
(UIA-хук), который на этой машине **НЕ собирается — нет .NET**. Поток:
[index.ts:249](apps/client/main/index.ts) `startSkillRecording`→`sc.startDemo()` (запрос к сайдкару)
→ события `demo` пушатся в `skillRec.events` → `stopDemo()` → `sendDemoSave(name, events)`.
Без сайдкара событий **ноль** → на сервере [record.ts:47](apps/server/src/brain/skills/record.ts)
`draft.steps.length === 0` → «не содержит значимых шагов — не сохраняем» → `null`, **тихий провал**.
Плюс текущий дизайн вообще не «анализирует экран» — опирался на UIA-события, не на зрение.

**Требование пользователя:** «анализировать экран И одновременно слушать пояснения к действиям».

**ПЛАН РЕДИЗАЙНА (vision+voice, без .NET):**
1. «сделай навык X» → demo-режим (новый флаг состояния).
2. Во время показа: периодический `screen.capture` (скриншоты-шаги) + ПАРАЛЛЕЛЬНО держать STT
   на голосовых пояснениях пользователя (нарратив того, что он делает).
3. На «готово»: **Opus 4.8 (vision)** синтезирует ПРОЦЕДУРУ из скриншотов + нарратива →
   `skill_save` ([skills.ts:538](apps/server/src/memory/skills.ts) `save`/`serializeLearnedSkill`).
4. Маршрутизировать демонстрацию в **HERMES learned-path** (текст-процедура, которой LLM СЛЕДУЕТ),
   а НЕ в детерминированный UIA-replay (`demoEventsToSteps`).
- **Файлы:** [index.ts](apps/client/main/index.ts) `startSkillRecording`/`stopSkillRecording`
  (~249–292 — заменить sidecar на таймер скриншотов + сбор STT-нарратива);
  [demo.ts](apps/server/src/brain/skills/demo.ts)/[record.ts](apps/server/src/brain/skills/record.ts)
  (vision-синтез вместо `demoEventsToSteps`); `packages/protocol` (форма demo.record);
  [persona.md](apps/server/src/brain/persona/persona.md) (как вести демонстрацию голосом).
- **Промежуточно (дёшево):** сделать провал ГРОМКИМ — голосом «не смог записать показ, сайдкар
  недоступен» вместо тишины, чтобы пользователь не гадал.

---

## Голос — устойчивость на дни/год (находки ревью; НЕ исправлены)
Многоагентное ревью дало 23 находки; верификация/синтез погибли в 500-шторме API. Перепроверить
и внедрить (полные транскрипты: `subagents/workflows/wf_00345010-d81/`):
- **P1 `no-network-reconnect-in-stream`** — Deepgram WS не переподключается при сетевом сбое
  посреди стрима → на дни рвётся и не встаёт. Добавить reconnect с backoff в [deepgram.ts](apps/server/src/integrations/deepgram.ts).
- **P1 `transport-reconnect-no-pong-on-reschedule`** — реконнект транспорта не пере-армит
  heartbeat/pong → мёртвые соединения накапливаются. [transport/index.ts](apps/client/main/transport/index.ts).
- **P1 `deepgram-reopen-per-turn` / `persistent-stt-ws` / `lost-first-frames-2nd-phrase`** —
  WS переоткрывается КАЖДЫЙ ход (`finalizeStt` рвёт стрим на каждом эндпоинте; в server.log
  `deepgram WS закрыт {code:1000}` после КАЖДОЙ реплики). Churn + ~0.3-0.5с латентности/ход +
  риск потери первых кадров. ЭТО — следующий главный шаг к «держит связь как другие чаты»:
  держать ОДИН WS на разговор (KeepAlive уже есть), эндпоинтить по is_final/VAD, рвать только
  на конце окна/сессии. Не сделано в этой сессии (риск, целил в felt-баг сначала).
- **P2 `cadence-history-unbounded` / `inmemory-episodic-unbounded`** — массивы/Map растут без
  вытеснения → рост памяти за дни. Ввести cap/eviction.
- **✅ `vad-flap-no-debounce` / `vad-dither-floods-server-turn-detector`** — СДЕЛАНО: онсет-дебаунс
  `onsetFrames=3` в [vad/index.ts](apps/client/main/vad/index.ts) (+тесты). Дребезг устранён.
- **P3** `gen-monotonic-growth-benign`, `awake-window-grows-stale-multiturn`,
  `silence-timer-followup-overlap`.
- **Проверить отдельно `server-agc-target-cements-deafness`** — нет ли СЕРВЕРНОГО gain/target-level
  (в deepgram/pipeline), который тоже фиксирует тихий сигнал (мог усиливать AGC-эффект).

---

## Как запускать (что реально работало этой сессии)
- **Сервер:** `cd apps/server` → `Start-Process pnpm dev -WorkingDirectory <abs> -RedirectStandardOutput
  ..\..\server.log -RedirectStandardError ..\..\server.err.log -WindowStyle Hidden`. Ждать `healthz`
  (порт 8787) ≈2с. tsx-watch может **молча умереть** → клиент `ECONNREFUSED 127.0.0.1:8787`; просто
  поднять заново (порт освободится).
- **Клиент:** `pnpm --filter @jarvis/client build` → `cd apps/client` → `Start-Process pnpm start
  -RedirectStandardOutput ..\..\client.out.log -RedirectStandardError ..\..\client.err.log -WindowStyle Hidden`.
  НОВЫЙ клиент пишет в **client.out.log** (stdout); `client.log` — вчерашний, не путать.
- **Логи:** читать через Read/grep/`tail` (Bash), **НЕ** PowerShell `Get-Content` — кириллица в пути
  `Автокомп` ломает парсинг команды. Пики речи: `grep peak server.log`.
- **Проверки:** `pnpm -r typecheck`; `pnpm --filter @jarvis/server test`;
  `@jarvis/tools test`; `@jarvis/client test` (audio-тесты включены).

## Чего НЕ делать (требования/грабли)
- Модель — **только Opus 4.8**; TTS — **ElevenLabs multilingual_v2**; STT — облако (Deepgram/Whisper).
- **AEC НЕ отключать** — нужен против barge-in на собственный TTS (AGC/NS — можно, см. БАГ 1).
- **.NET на машине НЕТ** — C#-сайдкар не вариант; демонстрацию делать на vision+voice (см. БАГ 2).
- НЕ слать temperature/top_p на Opus 4.8 (HTTP 400).
- НЕ хардкодить под сервис — общие руки + навыки + LLM решает (безопасность — из экспертизы агента,
  не из моих блок-листов).
- НЕ давать агенту закрывать/убивать себя и критические процессы (уже защищено: app.close + self-exclusion,
  Alt+F4 заблокирован, kill-guard в code.run).
- НЕ частить рестартами (флаппинг хендшейков).
