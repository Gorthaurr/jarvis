# NEXT_SESSION — с чего начать (обновлено 2026-07-10, поздний вечер)

> 🔴 **ЧИТАТЬ ПЕРВЫМ: [docs/HOW_IT_WORKS.md](docs/HOW_IT_WORKS.md)** — механизмы + как тестировать текстом + мышление.
> Ветка: `feat/native-pg-pgvector-cache-gateway`, PR #1 (открыт; GitGuardian красный из-за секрета
> в ИСТОРИИ ветки — пароль локальной БД; решение за владельцем: resolve в дашборде / rewrite / merge с игнором).

## ГЛАВНАЯ ЗАДАЧА СЛЕДУЮЩЕЙ СЕССИИ (запрос владельца)

**Концепт «своя среда»: дать модели делать НА ПК всё — быстро.** Владелец спросил: «мб стоит придумать
свою среду и концепт, который позволит модели делать абсолютно всё на компе». Согласованное направление
(обсуждено в сессии 2026-07-10, владелец ждёт проработку):

1. **НЕ песочница/VM** — Джарвис живёт на личном ПК владельца (его логины/файлы/игры); изоляция убивает смысл.
2. Ядро — 4 сдвига: (а) наблюдение = ПОДПИСКА (WinEvent/UIA-events/watchers → «разбуди, когда появится
   окно X»), не опрос скриншотами; (б) **world model** — живое дерево окна→элементы→состояние на сервере,
   обновляемое событиями; инструменты = дешёвые запросы к нему; (в) **persistent Джарвис-SDK** — живой
   Python/PS-интерпретатор с СОСТОЯНИЕМ между вызовами и API вида `jarvis.window("Dota").click(...)`,
   `jarvis.wait_for(text="Принять")`: один LLM-раунд = целая процедура, не один клик (ГЛАВНЫЙ скачок,
   единственное, что реально выходит за рамки Волн 2-3); (г) среда наращивает свой API — выученные
   HERMES-процедуры компилируются в именованные функции SDK (`jarvis.dota.accept()`).
3. (а)-(б) = радикализация Волн 2-3 из HARNESS_PLAN; (в)-(г) — новое. Безопасность — capability-модель
   (явное намерение владельца = токен на необратимое), не урезание мощи.
4. Честные пределы, которые среда не чинит: реалтайм-рефлексы игр, качество STT, недетерминизм чужих GUI.

Старт: либо концепт-документ в docs/ (этапы/риски), либо СНАЧАЛА deep-research по persistent-execution
и world-model агентам 2025-26 (Claude Agent SDK, CUA-harness'ы) — владелец выбор не озвучил; рисёрч
надёжнее (концепт на фактах, не на голове).

## Что сделано 2026-07-10 (всё закоммичено, работает живьём, сервер+клиент на этом коде)

1. **Волна 1 harness'а** (план+аудит 38 агентов: `docs/HARNESS_PLAN_2026-07-10.md`): earcon-приёмка
   (<0.5с вместо 8с тишины), дубль-гейт STT-обрывков (полные токены + e5 на фрагментах ≤3 токенов,
   порог 0.86 ПО ЖИВЫМ ЗАМЕРАМ — e5 НЕ разделяет команды одного домена, порог общего вида опасен),
   очередь за арендой не сжигает потолок задачи + acquireWithTimeout(60с) + гард протухшего клика,
   ui_ground/ui_invoke горячие (ui_ground=neutral-чтение, разрешён в браузерных задачах), бюджет-нудж
   на 70% потолка + ранний свёрток, роутер-гейт обрывков (внутритокенный микс/предложный фрагмент →
   clarify «повторите» за $0), кеш-гигиена (KEEP_SCREENSHOTS=1, family-boost=2 раунда, 1h-тариф 2×,
   changelog персоны вынесен из префикса −3K ток), пер-раундовая телеметрия + WARN «перезапись префикса».
2. **Память+контекст, уровень А + Б4а/Б5** (отчёт: `docs/MEMORY_CONTEXT_REVIEW_2026-07-10.md`):
   **facts:0 закрыт** — баг спреда (retrieval затирал профильные факты) + единый писатель
   `memory/user-memory.ts` (дедуп ≥0.93 + мост в профиль, кап 20) + рефлекс-бэкстоп
   `brain/agent/memory-reflect.ts` (маркер «я всегда/мой брат/аллергия» → дешёвый LLM с [memory_write],
   кап 8/день, spend-учёт) + persona v71 (позитивные триггеры записи). Живьём: первые факты в профиле
   («Работает по ночам. Не любит созвоны по утрам»), модель И рефлекс пишут, дедуп ловит.
   **Контекст ПК ожил**: заголовки ВСЕХ окон (группировка по процессу), fullscreen из PS
   (MonitorFromWindow — mixed-DPI мультимонитор, Electron-пересчёт был сломан), presence-строка
   («за ПК/отошёл; полноэкранно: X») + сенсоры §9 запитаны (гейт «не мешать в игре» работает),
   Steam-игры в env (живьём: Dota 2), env TTL 6ч, онбординг-кулдаун 6ч (конец «привет ×7/день»).
   **Управление задачами по userId**: «отмени/пауза/статус» переживают reconnect (cancelUser;
   живой тест: новая сессия отменила задачу старой) + голосовой перехват управления (был ТОЛЬКО на
   dev.text — живой голос уходил в агент!) + отказ всегда озвучивается (конец «немой смерти»).
   **Голос**: VAD-потолок «речи» 20с + адаптивный порог под громкий фон (конец 39-минутных ходов;
   спад ТОЛЬКО на тихих кадрах — ревью поймало цикл), DEEPGRAM_KEYTERM=1 (живьём Results идут),
   wake second-chance «Вы мне, сэр?» на near-miss ≤4 при активной задаче (ФЛАГ подтверждения
   «да/тебе» ≤2 токенов, окно разговора НЕ открывается — ревью закрыло дыру «трёп станет командой»).
   В БД: 141 июньский STT-event помечен stale (обратимо).
3. Все пакеты прошли адверсариальные ревью (5 ревьюеров; финальное дало 4 HIGH — все закрыты:
   fullscreen→PS, second-chance→флаг, голосовой перехват, немой pause/status).

## Открытое (кроме главной задачи)

- **Волна 2** HARNESS_PLAN: fused act+observe (−40% раундов, главный множитель), input_batch,
  дешёвые сенсоры (кроп/OCR/wait_for), сайдкар (ui.snapshot/window.list/полная мышь),
  admission-очередь GUI-задач, STT-нормализатор лексики, пер-раундовый thinking.
- **Уровень Б** MEMORY_CONTEXT_REVIEW: Б1 «сон-цикл» (консолидация working→facts раз в день, ~$0.02),
  Б2 стабильные факты отдельным КЕШИРУЕМЫМ блоком + skip retrieval на пустом сторе, Б3 live-рефреш
  хвостом в длинных задачах, Б4б-д resume-санация (fail-fast мёртвого сокета — задачи-зомби жгли $0.68;
  rebind задач на новую сессию; channel_down ≠ триггер эскалации), Б6 бюджет разговорного хода
  (комплимент стоил $0.19 и 8 раундов).
- Мелочь из ревью (не чинилось, низкий приоритет): greeting/watch-checker не учитывают траты в
  SpendGuard (существующая дыра); дефолтная Steam-библиотека сканится дважды (лишний IO);
  presence-строка не шлётся при пустом окно-снимке; лексический CANCEL_WORDS без «останови»
  (осознанно: конфликт с медиа-паузой).
- Мониторинг keyterm: если Deepgram «замолчит» (0 Results при идущих кадрах) — убрать
  `DEEPGRAM_KEYTERM=1` из `.env` (подозрение описано в deepgram.ts:112).

## Как проверять (напоминание)
Текст-драйвер: `node _jarvis_cmd.mjs "фраза"` (СВОЯ сессия, dev-token `cmd-test` — онбординг не звучит).
Сервер: `apps/server` → `npx tsx src/index.ts`. Клиент: `pnpm --filter @jarvis/client start` (PowerShell).
Логи: `apps/server/data/logs/server-YYYY-MM-DD.log` + `metrics.jsonl` (теперь и пер-раундовые type:"round").
