# Следующая сессия — с чего начать

Обновлено 2026-09-24 (сессия «фулл ревью»). Вердикт и все находки — [`REVIEW_2026-09-24.md`](REVIEW_2026-09-24.md).
Прошлый план (01.09) — [`NEXT_SESSION_2026-09-01.md`](NEXT_SESSION_2026-09-01.md).

## 0. Состояние на входе
- Ветка `feat/review-2026-09-24` (включает W0–W4 от 09–10.09) — PR в `main`, см. ссылку в REVIEW §6.
- Супервизор (`JarvisSupervisor`) держит сервер И клиент. Проверка: `infra/supervisor.log`, `apps/client/client.out.log`.
- Тесты: сервер ~3000, клиент ~765, typecheck чистый.
- Данные почищены 24.09 (резерв — `apps/server/data/_backup_2026-09-24/`): рабочая память, dev-задачи, фикстуры
  тестов, 5 испорченных навыков, напоминания «самопроверка» и «пора сдавать отчёт».

## 1. Шаги владельца (без них часть не работает)
1. Расширение Chrome: `chrome://extensions` → «Загрузить распакованное» → `C:\Users\anton\Desktop\autokomp\jarvis\apps\extension`
   (ID `pjkeladocehklaefmnhapmmpabmaeajd`). Без этого нет браузерных рук, почты, календаря.
2. Решить: мягкий гейт диктора (`JARVIS_SPEAKER_GATE=1`) — да/нет.

## 2. Первым делом в следующей сессии
1. **Живой прогон рук** (нужен доступ computer use к рабочему столу или голос владельца — 24.09 владелец доступ не дал):
   «открой дискорд» (фикс аргументов ярлыка), «открой блокнот и напиши привет от Джарвиса» (act type+verify),
   «запусти доту» → «нажми Играть» (act по OCR в игре, H-A1), Telegram Desktop: найти чат (без отправки), Проводник.
   Это и есть приёмочный тест §8.2 плана 09.09 — ни разу не гонялся.
2. Переписать 15 выученных навыков, которые учат холодным `input_click`/`app_focus`/`ui_snapshot`, на `act`/`window`/`look`.
3. Дожать скорость на подписке: нудж-раунды продолжают ту же SDK-сессию (сейчас — новый CLI, +16 с), эскалация §7 без
   сброса сессии, ack разговорного хода, ушедшего в инструменты (B-F7/F9/F10).
4. Метрика `first_answer` (сейчас `mouth_to_ear` меряет ack).

## 3. Грабли этой сессии
- Команды агента идут через песочницу: Electron из неё падает на GPU-песочнице — это артефакт, у владельца нет.
  «Работает ли у владельца» — только через супервизор (авто-память `agent-sandbox-electron-artifact`).
- Включить `JARVIS_DEV_HTTP` для `/dev/say` автоклассификатор агента запрещает (ослабление защиты) — живые GUI-прогоны
  через computer use или голос владельца.
- Кеши моделей — вне `node_modules`; распакованное расширение — с `key` в manifest.
