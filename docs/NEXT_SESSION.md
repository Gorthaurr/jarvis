# Следующая сессия — с чего начать

Обновлено 2026-09-24 (сессия «фулл ревью», после двух раундов адверс-ревью). Вердикт и все находки —
[`REVIEW_2026-09-24.md`](REVIEW_2026-09-24.md) (§6 — ревью правок). Прошлый план (01.09) — [`NEXT_SESSION_2026-09-01.md`](NEXT_SESSION_2026-09-01.md).

## 0. Состояние на входе
- Ветка `feat/review-2026-09-24` (включает W0–W4 от 09–10.09) — PR в `main`, ссылка в REVIEW §7.
- Супервизор (`JarvisSupervisor`) держит сервер И клиент. Проверка: `infra/supervisor.log`, `apps/client/client.out.log`.
- Тесты: сервер ~3020, клиент ~775, typecheck чистый.
- Данные почищены 24.09 (резерв — `apps/server/data/_backup_2026-09-24/`): рабочая память, dev-задачи, фикстуры,
  5 испорченных навыков, напоминания «самопроверка» и «пора сдавать отчёт»; 10 личных навыков переписаны на
  `act`/`look`/`window` (прежние версии — `_backup_2026-09-24/skills_pre_rewrite/`).

## 1. Шаги владельца (без них часть не работает)
1. Расширение Chrome: `chrome://extensions` → «Загрузить распакованное» → `C:\Users\anton\Desktop\autokomp\jarvis\apps\extension`
   (ID `pjkeladocehklaefmnhapmmpabmaeajd`). Без этого нет браузерных рук, почты, календаря.
2. Решить: мягкий гейт диктора (`JARVIS_SPEAKER_GATE=1`) — да/нет.

## 2. Первым делом в следующей сессии
1. **Живой прогон рук** (нужен доступ computer use к рабочему столу или голос владельца — 24.09 доступ не дан):
   «открой дискорд» (аргументы ярлыка), «открой блокнот и напиши привет от Джарвиса» (act type+verify, System32-блокнот),
   «запусти доту» → «найди игру» (act по OCR в игре, навык v7 без координатного макроса), Telegram Desktop: найти чат
   (без отправки), «открой диспетчер задач / командную строку / параметры». Это приёмочный тест §8.2 плана 09.09.
2. Скорость на подписке: нудж-раунды в той же SDK-сессии (сейчас новый CLI, +16 с), эскалация §7 без сброса сессии,
   ack разговорного хода, ушедшего в инструменты (B-F7/F9/F10). Метрика `first_answer` (сейчас `mouth_to_ear` меряет ack).
3. Печать через SDK-мост (`code_run` + `jarvis.write`) идёт мимо серверного гарда паролей/карт — клиентский рубеж
   (`commit-guard.ts`) проверяет только коммит-клавиши. Решить: гард печати на клиенте или запрет `write` в
   рискованных окнах.
4. Уточнение консьержа (`interceptClarify`): при откате в модель причина провала tier0 не доходит до хода (`priorFailure`).

## 3. Грабли этой сессии
- Команды агента идут через песочницу: Electron из неё падает на GPU-песочнице — это артефакт, у владельца нет.
  «Работает ли у владельца» — только через супервизор (авто-память `agent-sandbox-electron-artifact`).
- Включить `JARVIS_DEV_HTTP` для `/dev/say` автоклассификатор агента запрещает (ослабление защиты) — живые GUI-прогоны
  через computer use или голос владельца.
- Кеши моделей — вне `node_modules`; распакованное расширение — с `key` в manifest.
- Правки через `node -e`/heredoc съедают `\` (регэкспы `\p{L}`, `\r\n`) — для текста с обратными слэшами Edit или файл-скрипт.
- Строка прозы навыка, начинающаяся с `verify`/`wait`/`launch`/`focus`, парсится в шаг слепого реплея (страж — `seed/shared-skills.test.ts`).
