-- =============================================================================
-- ПРОДУКТОВЫЙ КАРКАС (2026-09-02, ревью): списание кредитов пакетов и полный кап периода.
-- Ревью нашло: кредиты пакета НИКОГДА не списывались — кап каждого нового месяца снова включал весь
-- пакет (двойной учёт). Теперь перерасход сверх квоты плана списывается с грантов (credits.consumeCredits),
-- а сколько списано в этом периоде — здесь; кап периода = квота + остаток + списанное в периоде.
-- cap_micro — полный кап (квота+кредиты) для отчёта «Квоты» (раньше % считался от одной квоты плана).
-- =============================================================================
ALTER TABLE usage_quota ADD COLUMN IF NOT EXISTS credits_consumed_micro BIGINT NOT NULL DEFAULT 0;
ALTER TABLE usage_quota ADD COLUMN IF NOT EXISTS cap_micro BIGINT;

-- BYO-план продаётся только с LLM-прокси проекта (ключ пользователя идёт своему провайдеру); без прокси запросы
-- шли бы на ключ проекта за план, не покрывающий COGS. Включить обратно: UPDATE plans SET active = TRUE WHERE id = 'byo'.
UPDATE plans SET active = FALSE WHERE id = 'byo';
