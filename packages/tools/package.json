{
  "name": "@jarvis/tools",
  "version": "0.1.0",
  "private": true,
  "description": "JSON Schema определения инструментов мозга в формате Anthropic tool-use (§6 актуаторы, §12 web-инструменты, §8 память). Гарды §14 закодированы в описаниях.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@jarvis/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
