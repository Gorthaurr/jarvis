/**
 * Курируемая СТАРТОВАЯ библиотека ОБЩИХ навыков (§мультитенант, Фаза 1).
 *
 * Эти процедуры засеваются в общую библиотеку (SHARED_USER_ID) на boot и видны ВСЕМ юзерам через
 * recall — чтобы новому пользователю не пришлось «обучать своего Джарвиса» базовым приёмам с нуля.
 * Растёт дальше через `skill_promote` (реально сработавшие личные навыки → в общие).
 *
 * Содержимое НАМЕРЕННО консервативное и СИСТЕМО-СПЕЦИФИЧНОЕ (не generic-советы из интернета, а
 * накопленное know-how именно этого Джарвиса — встроенные инструменты + грабли). Бамп `version`
 * при правке процедуры → seedSharedSkills перезальёт (по версии). Меняешь приём — бампни version.
 */
import { serializeLearnedSkill } from "../memory/skills.js";

/** Описания общих навыков (id с префиксом learned__ — пространство выученных процедур). */
const SEED = [
  {
    id: "learned__site-player-control",
    name: "Управление плеером на сайте (YouTube/Я.Музыка)",
    version: 1,
    when:
      "Просят включить/поставить на паузу/переключить воспроизведение на открытом сайте — YouTube, " +
      "Яндекс.Музыка, веб-плеер. Не про системные медиа-клавиши, а именно про управление вкладкой.",
    procedure: [
      "1. Действуй в УЖЕ открытой вкладке через расширение (browser_act), а НЕ мышью и НЕ системной",
      "   медиа-клавишей: SMTC-клавиша может уйти в другое приложение/игру.",
      "2. Кнопку play/pause ищи по aria-label на РУ и EN: «Воспроизведение»/«Пауза»/«Play»/«Pause»",
      "   (на боевой Я.Музыке media()-элемента нет — это MSE, нужна именно глобальная кнопка плеера).",
      "3. Идемпотентность: если уже играет, повторное «включи» не должно ломать — сверься с состоянием.",
      "4. ПРОВЕРЬ ИСХОД перед «готово»: aria-label кнопки должен флипнуться (Воспроизведение→Пауза).",
      "   Не флипнулся — autoplay мог заблокировать; честно скажи «нужен живой клик по вкладке», не ври «играет».",
      "5. Вкладки сервиса нет — сначала открой её (browser_open с фокусом существующей, без дублей).",
    ].join("\n"),
  },
  {
    id: "learned__telegram-send",
    name: "Отправить сообщение или голосовое в Telegram",
    version: 1,
    when: "Просят написать/отправить кому-то в Telegram (текст или голосовое сообщение).",
    procedure: [
      "1. Текст — инструментом telegram_send{to,text}; голосовое — telegram_send_voice (запишется",
      "   голосом, синтез на сервере). Отправка НЕВИДИМАЯ, фоновой вкладкой — окно не выпрыгивает.",
      "2. Получателя по имени резолвит сервер (в т.ч. транслит Герман↔Herman); при неоднозначности",
      "   реши по контексту диалога, не угадывай молча — лучше короткий уточняющий вопрос.",
      "3. Необратимое действие (отправка) — пройдёт подтверждение (§14); это нормально, дождись его.",
      "4. После успеха коротко подтверди. Сбой расширения/не найден чат — честно сообщи, не «готово».",
    ].join("\n"),
  },
] as const;

/** content_md общих навыков для seedSharedSkills (канонический формат learned-процедуры). */
export const SHARED_SKILL_SEED: readonly string[] = SEED.map((s) =>
  serializeLearnedSkill({ id: s.id, name: s.name, version: s.version, when: s.when, procedure: s.procedure }),
);
